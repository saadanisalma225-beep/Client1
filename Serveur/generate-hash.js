// // Fichier temporaire : generate-hash.js
// const bcrypt = require('bcryptjs');

// async function hashPassword(password) {
//   const hash = await bcrypt.hash(password, 12);
//   console.log(hash);
// }

// hashPassword('admin_pfe2026');